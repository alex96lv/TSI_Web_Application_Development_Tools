# 🚀 Инструкция по установке

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd "Final Project"
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
# Python зависимости
pip install -r requirements.txt

# Node.js зависимости (для SASS)
npm install
```

### 4. Настройка переменных окружения

Скопируйте файл примера и настройте под свои нужды:

```bash
cp .env.example .env
```

Откройте `.env` и измените:
- `SECRET_KEY` - установите свой секретный ключ
- `DEBUG` - установите `False` для production
- `ALLOWED_HOSTS` - добавьте ваш домен
- MongoDB настройки (если отличаются от localhost)

### 5. Запуск MongoDB

**Через Docker Compose (рекомендуется):**
```bash
docker-compose up -d
```

**Через Docker вручную:**
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

**Локальная установка (Ubuntu/Debian):**
```bash
sudo systemctl start mongod
```

### 6. Компиляция SASS

```bash
npx sass static/scss/style.scss static/css/style.css
```

### 7. Запуск проекта

**Автоматический запуск (MongoDB + Django):**
```bash
./start.sh
```

**Или вручную:**
```bash
source venv/bin/activate
python manage.py runserver
```

Приложение будет доступно по адресу: **http://localhost:8000**

## 🔧 Админские скрипты

### Сброс пароля пользователя

```bash
./reset_password.sh
```

### Обновление категорий

```bash
./update_categories.sh
```

## 🛑 Остановка проекта

```bash
./stop.sh
```

Или вручную:
```bash
# Остановить Django (Ctrl+C)
# Остановить MongoDB
docker-compose down
```

## 📝 Примечания

- При первом запуске создастся база данных автоматически
- Категории создаются автоматически при регистрации первого пользователя
- Логи находятся в папке `logs/`
- Для production обязательно измените `SECRET_KEY` и установите `DEBUG=False`

## 🆘 Устранение неполадок

### MongoDB не запускается

Проверьте, что порт 27017 свободен:
```bash
sudo lsof -i :27017
```

### Ошибка "ModuleNotFoundError"

Убедитесь, что виртуальное окружение активировано:
```bash
source venv/bin/activate
```

### SASS не компилируется

Установите Node.js зависимости:
```bash
npm install
```
