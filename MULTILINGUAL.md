# 🌍 Мультиязычность / Multilingual Support

Проект поддерживает **3 языка**:
- 🇷🇺 **Русский (ru)** - язык по умолчанию
- 🇬🇧 **English (en)**
- 🇱🇻 **Latviešu (lv)**

## 🎯 Возможности

✅ **Переключение языка в реальном времени** - без перезагрузки страницы
✅ **Автоматическое сохранение** - выбранный язык сохраняется в localStorage
✅ **Полный перевод интерфейса** - все элементы переведены
✅ **Простое добавление новых языков** - через файл i18n.js

## 🚀 Как использовать

### Переключение языка

Выберите язык в выпадающем списке в правом верхнем углу навигационной панели:

```
🇷🇺 Русский
🇬🇧 English
🇱🇻 Latviešu
```

Все тексты на странице мгновенно переведутся на выбранный язык!

## 📁 Структура переводов

Все переводы хранятся в файле: **[static/js/i18n.js](static/js/i18n.js)**

### Формат переводов

```javascript
const translations = {
    ru: {
        'key': 'Русский текст',
        // ...
    },
    en: {
        'key': 'English text',
        // ...
    },
    lv: {
        'key': 'Latviešu teksts',
        // ...
    }
};
```

### Категории переводов

1. **Navigation** - навигационное меню
2. **Auth** - аутентификация (вход, регистрация)
3. **Dashboard** - главная панель
4. **Transactions** - транзакции
5. **Budgets** - бюджеты
6. **Goals** - цели
7. **Profile** - профиль
8. **Common** - общие элементы

## 🛠️ Как добавить перевод для нового элемента

### 1. Добавить текст в HTML с атрибутом `data-i18n`

```html
<h1 data-i18n="my_new_key">Текст по умолчанию</h1>
```

### 2. Добавить переводы в i18n.js

```javascript
const translations = {
    ru: {
        'my_new_key': 'Мой новый текст',
    },
    en: {
        'my_new_key': 'My new text',
    },
    lv: {
        'my_new_key': 'Mans jaunais teksts',
    }
};
```

### 3. Страница автоматически обновится!

## 📝 Примеры использования

### В HTML шаблонах

```html
<!-- Обычный текст -->
<h1 data-i18n="dashboard">Главная панель</h1>

<!-- Кнопки -->
<button data-i18n="save">Сохранить</button>

<!-- Ссылки -->
<a href="#" data-i18n="nav_logout">Выход</a>

<!-- Placeholder для input -->
<input type="text" data-i18n="email" placeholder="Email">
```

### В JavaScript коде

```javascript
// Получить перевод
const text = t('my_key');

// Изменить язык программно
setLanguage('en');

// Получить текущий язык
const currentLang = getCurrentLanguage();
```

## 🌐 Добавление нового языка

### Шаг 1: Добавить в settings.py

```python
LANGUAGES = [
    ('ru', 'Русский'),
    ('en', 'English'),
    ('lv', 'Latviešu'),
    ('de', 'Deutsch'),  # Новый язык
]
```

### Шаг 2: Добавить переводы в i18n.js

```javascript
const translations = {
    // ... существующие языки
    de: {
        'app_title': '📊 Ausgaben-Tracker',
        'nav_dashboard': 'Dashboard',
        // ... все остальные ключи
    }
};
```

### Шаг 3: Добавить в language selector

```html
<select id="language-selector">
    <option value="ru">🇷🇺 Русский</option>
    <option value="en">🇬🇧 English</option>
    <option value="lv">🇱🇻 Latviešu</option>
    <option value="de">🇩🇪 Deutsch</option>
</select>
```

## 🔧 API функции

### `getCurrentLanguage()`
Получить текущий выбранный язык из localStorage

```javascript
const lang = getCurrentLanguage(); // 'ru', 'en', 'lv'
```

### `setLanguage(lang)`
Установить новый язык

```javascript
setLanguage('en'); // Переключить на английский
```

### `t(key)`
Получить перевод по ключу

```javascript
const text = t('dashboard'); // "Dashboard" для en, "Главная панель" для ru
```

### `translatePage()`
Перевести всю страницу (вызывается автоматически)

```javascript
translatePage(); // Обновить все переводы на странице
```

## 📊 Статистика переводов

| Категория | Ключей | Статус |
|-----------|--------|--------|
| Navigation | 6 | ✅ Переведено |
| Auth | 11 | ✅ Переведено |
| Dashboard | 6 | ✅ Переведено |
| Transactions | 13 | ✅ Переведено |
| Budgets | 8 | ✅ Переведено |
| Goals | 7 | ✅ Переведено |
| Profile | 5 | ✅ Переведено |
| Common | 6 | ✅ Переведено |
| **Всего** | **62 ключа** | **✅ 100%** |

## 🎨 Примеры переводов

### Русский (RU)
- Главная панель
- Доходы за месяц
- Расходы за месяц
- Добавить транзакцию

### English (EN)
- Dashboard
- Income this month
- Expenses this month
- Add Transaction

### Latviešu (LV)
- Galvenais panelis
- Ienākumi šajā mēnesī
- Izdevumi šajā mēnesī
- Pievienot transakciju

## 💡 Рекомендации

1. **Используйте короткие ключи** - например, `nav_dashboard` вместо `navigation_dashboard_link`
2. **Группируйте по категориям** - nav_, auth_, dashboard_, и т.д.
3. **Добавляйте комментарии** - для сложных переводов
4. **Тестируйте на всех языках** - переключайтесь и проверяйте каждую страницу
5. **Используйте Unicode** - для флагов и эмодзи

## 🐛 Решение проблем

### Текст не переводится
- Проверьте, что добавлен атрибут `data-i18n="key"`
- Убедитесь, что ключ существует во всех языках в i18n.js
- Проверьте консоль браузера на ошибки

### Язык не сохраняется
- Проверьте localStorage в DevTools (Application → Local Storage)
- Убедитесь, что JavaScript не блокируется

### Переводы не обновляются
- Очистите кэш браузера (Ctrl+F5)
- Перезагрузите страницу

## 📦 Логирование

Логи находятся в папке **logs/**:
- `django.log` - общие логи приложения
- `errors.log` - ошибки приложения

### Просмотр логов

```bash
# Последние 50 строк общего лога
tail -n 50 logs/django.log

# Отслеживание ошибок в реальном времени
tail -f logs/errors.log

# Поиск по логам
grep "ERROR" logs/django.log
```

---

**Мультиязычность полностью реализована и готова к использованию!** 🎉

Переключайте языки в любой момент и наслаждайтесь интерфейсом на родном языке! 🌍
