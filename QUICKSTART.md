# 🚀 Быстрый старт

## Установка и запуск

Проект использует скрипт автоматического запуска:

```bash
./start.sh
```

Этот скрипт автоматически:
- Запустит MongoDB через Docker Compose
- Активирует виртуальное окружение
- Скомпилирует SASS стили
- Запустит Django сервер

Приложение будет доступно по адресу: **http://localhost:8000**

**Остановка проекта:**
```bash
./stop.sh
```

> 📖 **Подробная инструкция по установке** (если нужна ручная настройка): см. [README.md](README.md) раздел "🔧 Установка и запуск"

## 📝 Первый запуск

1. Откройте http://localhost:8000
2. Нажмите **"Регистрация"**
3. Заполните форму:
   - Email: ваш email
   - Пароль: минимум 8 символов
   - Выберите валюту ($, €)
4. После регистрации вы автоматически войдёте в систему
5. Попадёте на Dashboard

## 🎯 Что попробовать

### Добавьте транзакцию
1. Перейдите в **"Транзакции"**
2. Нажмите **"+ Добавить"**
3. Заполните форму (тип, категория, сумма, описание)
4. Сохраните

### Создайте бюджет
1. Перейдите в **"Бюджеты"**
2. Нажмите **"+ Добавить"**
3. Выберите категорию расходов
4. Установите лимит (например, 15000)
5. Выберите период (неделя/месяц)

### Установите цель
1. Перейдите в **"Цели"**
2. Нажмите **"+ Добавить"**
3. Название (например, "Отпуск в Турции")
4. Целевая сумма (например, 100000)
5. Текущая сумма (сколько уже накоплено)
6. Срок достижения

### Посмотрите статистику
1. Вернитесь на **Dashboard**
2. Увидите:
   - Общий баланс за месяц
   - Графики расходов по категориям
   - Тренды доходов и расходов
   - Последние транзакции

## 🔧 Возможные проблемы

Если возникли проблемы с запуском, см. раздел "Подробная установка" в [README.md](README.md)

## 📊 API для тестирования

Используйте **Postman** или **curl** для тестирования API:

**Регистрация:**
```bash
curl -X POST http://localhost:8000/api/auth/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "password_confirm": "password123",
    "currency": "USD"
  }'
```

**Вход:**
```bash
curl -X POST http://localhost:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

**Получить транзакции (с токеном):**
```bash
curl -X GET http://localhost:8000/api/transactions/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## 🎨 Динамические элементы

Обратите внимание на следующие динамические функции:

1. **Интерактивные графики** - на Dashboard
2. **Модальные окна** - при добавлении транзакций/бюджетов/целей
3. **Живая фильтрация** - в списке транзакций
4. **Анимированные прогресс-бары** - в бюджетах и целях
5. **Автообновление статистики** - при добавлении транзакций
6. **Валидация форм** - проверка данных в реальном времени
7. **Адаптивный дизайн** - попробуйте изменить размер окна

## 📚 Дополнительная информация

Полная документация в файле [README.md](README.md)

## ✅ Чеклист для преподавателя

- ✅ Django 5.0.1
- ✅ MongoDB (NoSQL)
- ✅ REST API (Django REST Framework)
- ✅ JWT аутентификация
- ✅ SASS/SCSS препроцессор
- ✅ 7+ динамических элементов
- ✅ Модульная структура
- ✅ Безопасность (хеширование паролей, CORS)
- ✅ Лучшие практики веб-разработки

Удачи! 🚀
