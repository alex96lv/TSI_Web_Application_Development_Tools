# 📊 Трекер Расходов и Доходов

Динамическое веб-приложение для управления личными финансами с использованием современных технологий.

## 📋 Описание проекта

Полнофункциональное веб-приложение для учёта доходов, расходов, управления бюджетами и финансовыми целями.

## 🚀 Технологии

### Backend:
- **Django 5.0.1** - веб-фреймворк
- **Django REST Framework 3.14** - создание REST API
- **MongoDB** - NoSQL база данных
- **MongoEngine** - ODM для работы с MongoDB
- **JWT (djangorestframework-simplejwt)** - аутентификация
- **bcrypt** - хеширование паролей
- **CORS Headers** - поддержка CORS

### Frontend:
- **HTML5** - разметка
- **SASS/SCSS** - препроцессор стилей
- **JavaScript (Vanilla)** - клиентская логика
- **Chart.js 4.4** - визуализация данных
- **Fetch API** - запросы к серверу

### Инструменты:
- **Python 3.10+**
- **npm** - менеджер пакетов для Node.js
- **sass** - компилятор SASS
- **MongoDB** - база данных

## ✨ Функциональность

### 1. Мультиязычность 🌍
- ✅ **3 языка**: Русский, English, Latviešu
- ✅ Переключение языка в реальном времени (без перезагрузки!)
- ✅ Автоматическое сохранение выбранного языка
- ✅ 62+ переведённых элемента интерфейса
- ✅ Полная документация: [MULTILINGUAL.md](MULTILINGUAL.md)

### 2. Логирование 📝
- ✅ Автоматическое логирование всех событий
- ✅ Отдельные логи для ошибок
- ✅ Папка `logs/` с файлами:
  - `django.log` - общие логи
  - `errors.log` - только ошибки

### 3. Аутентификация
- ✅ Регистрация новых пользователей
- ✅ Вход с JWT токенами
- ✅ Безопасное хранение паролей (bcrypt)
- ✅ Защищённые API endpoints
- ✅ Восстановление пароля (3 способа):
  - 🔑 Секретный вопрос (при регистрации)
  - ⏰ Временный код в консоли (15 минут)
  - 👨‍💼 Админский скрипт `reset_password.py`
  - Подробнее: [PASSWORD_RESET.md](PASSWORD_RESET.md)
- ✅ Современный градиентный дизайн страниц авторизации
- ✅ Красивые анимации и эффекты при взаимодействии

### 4. Транзакции
- ✅ Добавление доходов и расходов
- ✅ Категоризация транзакций
- ✅ Фильтрация по типу, категории, дате
- ✅ Редактирование и удаление

### 5. Категории
- ✅ Предустановленные категории с красивыми иконками
- ✅ Создание пользовательских категорий
- ✅ Современные эмодзи-иконки (🍽️ 🚙 🏡 💚 🎭 📦)
- ✅ Красочная цветовая палитра для каждой категории
- ✅ Отдельные категории для доходов и расходов

### 6. Бюджеты
- ✅ Установка лимитов расходов по категориям
- ✅ Отслеживание прогресса (недельный/месячный)
- ✅ Визуализация с прогресс-барами
- ✅ Цветовая индикация (зелёный/жёлтый/красный)

### 7. Цели сбережений
- ✅ Создание финансовых целей
- ✅ Отслеживание прогресса
- ✅ Установка дедлайнов
- ✅ Расчёт оставшейся суммы

### 8. Статистика и аналитика
- ✅ Dashboard с основными показателями (📊 💳 💎)
- ✅ Графики расходов по категориям (круговая диаграмма)
  - Автоматическая цветовая палитра по категориям
  - Всплывающие подсказки с процентами
  - Красивые округлые маркеры в легенде
- ✅ Тренды доходов и расходов (линейный график)
- ✅ Последние транзакции
- ✅ Адаптивный дизайн графиков для светлой/темной темы

## 📁 Структура проекта

```
Final Project/
├── expense_tracker/           # Главный модуль Django
│   ├── settings.py           # Настройки проекта
│   ├── urls.py              # Главный роутинг
│   └── wsgi.py
├── users/                    # Приложение пользователей
│   ├── models.py            # Модель User
│   ├── serializers.py       # Сериализаторы
│   ├── views.py             # API views
│   ├── authentication.py    # JWT аутентификация
│   └── urls.py
├── categories/              # Приложение категорий
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── transactions/            # Приложение транзакций
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── budgets/                 # Приложение бюджетов
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── goals/                   # Приложение целей
│   ├── models.py
│   ├── serializers.py
│   ├── views.py
│   └── urls.py
├── templates/               # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   ├── login.html
│   ├── register.html
│   ├── dashboard.html
│   ├── transactions.html
│   ├── budgets.html
│   ├── goals.html
│   └── profile.html
├── static/                  # Статические файлы
│   ├── css/
│   │   └── style.css       # Скомпилированный CSS
│   ├── scss/
│   │   └── style.scss      # SASS исходники
│   └── js/
│       ├── auth.js         # Аутентификация
│       └── api.js          # API запросы
├── requirements.txt         # Python зависимости
├── package.json            # Node.js зависимости
├── .env                    # Переменные окружения (не в Git)
├── .env.example            # Пример конфигурации
├── .gitignore
├── reset_password.py       # Админский скрипт сброса пароля
├── reset_password.sh       # Wrapper для запуска скрипта
├── update_categories.py    # Скрипт обновления категорий
├── update_categories.sh    # Wrapper для обновления
├── start.sh               # Скрипт запуска проекта
├── stop.sh                # Скрипт остановки проекта
└── README.md
```

## 🔧 Установка и запуск

### Быстрый старт (Рекомендуется)

Проект использует Docker Compose для автоматического управления MongoDB. Просто выполните:

```bash
# Запуск проекта (MongoDB + Django сервер)
./start.sh
```

Приложение будет доступно по адресу: **http://localhost:8000**

```bash
# Остановка проекта
./stop.sh
```

### Подробная установка (пошагово)

#### Шаг 1: Создать виртуальное окружение Python

```bash
python3 -m venv venv
```
Это создаст изолированное окружение для Python-пакетов проекта.

#### Шаг 2: Активировать виртуальное окружение

```bash
source venv/bin/activate
```
После этого в начале строки терминала появится `(venv)`.

#### Шаг 3: Установить Python зависимости

```bash
pip install -r requirements.txt
```
Установятся Django, MongoDB драйвер, JWT и другие библиотеки.

#### Шаг 4: Установить Node.js зависимости (для SASS)

```bash
npm install
```
Установится компилятор SASS для стилей.

#### Шаг 5: Убедиться, что MongoDB запущен

MongoDB должен быть запущен в Docker контейнере. Проверьте:

```bash
docker ps | grep mongo
```

Если контейнер не запущен, запустите его:

```bash
docker-compose up -d
```

#### Шаг 6: Применить миграции Django

```bash
python manage.py migrate
```
Создаст необходимые структуры в базе данных.

#### Шаг 7: Запустить Django сервер

```bash
python manage.py runserver
```

#### Шаг 8: Открыть в браузере

Перейдите по адресу: **http://localhost:8000**

## 📊 API Endpoints

### Аутентификация
- `POST /api/auth/register/` - регистрация
- `POST /api/auth/login/` - вход
- `POST /api/auth/token/refresh/` - обновление токена
- `GET /api/auth/me/` - текущий пользователь
- `PUT /api/auth/update/` - обновление профиля

### Транзакции
- `GET /api/transactions/` - список транзакций
- `POST /api/transactions/` - создать транзакцию
- `GET /api/transactions/{id}/` - детали транзакции
- `PUT /api/transactions/{id}/` - обновить
- `DELETE /api/transactions/{id}/` - удалить
- `GET /api/transactions/stats/summary/` - статистика

### Категории
- `GET /api/categories/` - список категорий
- `POST /api/categories/` - создать категорию
- `GET /api/categories/{id}/` - детали
- `PUT /api/categories/{id}/` - обновить
- `DELETE /api/categories/{id}/` - удалить

### Бюджеты
- `GET /api/budgets/` - список бюджетов
- `POST /api/budgets/` - создать бюджет
- `PUT /api/budgets/{id}/` - обновить
- `DELETE /api/budgets/{id}/` - удалить

### Цели
- `GET /api/goals/` - список целей
- `POST /api/goals/` - создать цель
- `PUT /api/goals/{id}/` - обновить
- `DELETE /api/goals/{id}/` - удалить

## 🎨 Дизайн и UX

### 5+ Динамических элементов:
1. ✅ **Интерактивные графики** (Chart.js) - круговые и линейные диаграммы
2. ✅ **Модальные окна** - добавление/редактирование без перезагрузки страницы
3. ✅ **Динамическая фильтрация** - мгновенное обновление списков
4. ✅ **Прогресс-бары** - анимированные индикаторы бюджетов и целей
5. ✅ **Живая валидация форм** - проверка данных на клиенте
6. ✅ **Автоматическое обновление статистики** - real-time расчёты
7. ✅ **Адаптивный дизайн** - работает на всех устройствах

### Использованные SASS техники:
- Переменные для цветов и размеров
- Миксины для повторяющихся стилей
- Вложенность селекторов
- Функции для градиентов
- Media queries для адаптивности

## 🔧 Админские инструменты

### 1. Скрипт восстановления пароля

Запустите скрипт для управления паролями пользователей:

```bash
./reset_password.sh
```

**Доступные опции:**

1. **Генерация временного кода (рекомендуется)**
   - Создаёт 8-символьный код, действительный 15 минут
   - Пользователь использует код на странице `/password-reset/`
   - Безопасный способ для удалённой поддержки

2. **Прямой сброс пароля**
   - Устанавливает новый пароль напрямую
   - Требует физического доступа к серверу
   - Используйте с осторожностью

3. **Список всех пользователей**
   - Показывает email, имя и дату регистрации

**Пример использования:**

```bash
$ ./reset_password.sh

============================================================
🔐 Password Reset Admin Tool
============================================================

Choose an option:
1. Generate temporary reset code (recommended)
2. Direct password reset
3. List all users
4. Exit
============================================================

Enter your choice (1-4): 1
Enter user email: user@example.com

✅ Password Reset Code Generated!
📧 User: user@example.com
🔑 Reset Code: ABC12XYZ
⏰ Expires: 2026-01-06 18:15:00
```

Подробнее: [PASSWORD_RESET.md](PASSWORD_RESET.md)

### 2. Скрипт обновления категорий

Используйте для обновления иконок и цветов категорий:

```bash
./update_categories.sh
```

Скрипт автоматически обновит все существующие категории в базе данных с новыми современными иконками и цветами.

## 🛡️ Безопасность

- JWT токены для аутентификации
- Хеширование паролей (bcrypt)
- CORS настройки
- Валидация данных на сервере
- Защита от SQL-инъекций (NoSQL)
- Безопасные HTTP-only cookies (опционально)

## 📄 Лицензия

MIT License
